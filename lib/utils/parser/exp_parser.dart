class ExpParser {
  ExpParser._();

  static void checkSymbolInsertion(String operator, String expression) {
    switch (operator) {
      case '+':
      case '-':
      case '/':
      case '*':
      case '%':
      default:
    }
  }
}
